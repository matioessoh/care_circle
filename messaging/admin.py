from django.contrib import admin

# Private conversations and messages between users are confidential.
# They are intentionally NOT registered in Django admin to protect privacy.
# Only the messaging app manages the data directly.
